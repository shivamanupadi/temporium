{
  "name": "@temporium/typescript-config",
  "version": "1.0.0",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "lint": "echo 'No linting configured for typescript-config'",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "clean": "rimraf node_modules .turbo",
    "reset": "npm run clean && npm install"
  }
}
