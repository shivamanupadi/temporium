{
  "name": "@temporium/playground",
  "version": "0.1.0",
  "private": true,
  "description": "Tempo Playground - Contract development IDE for Tempo blockchain",
  "scripts": {
    "dev": "turbo run dev --filter=@temporium/playground-web",
    "build": "turbo run build --filter=@temporium/playground-web",
    "lint": "turbo run lint --filter=@temporium/playground-web",
    "clean": "turbo run clean --filter=@temporium/playground-web"
  }
}
