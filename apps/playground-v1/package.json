{
  "name": "@temporium/playground-v1",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "turbo run dev --filter=@temporium/wallet-api --filter=@temporium/wallet-web --filter=@temporium/wallet-demo-dapp --filter=@temporium/playground-v1-api --filter=@temporium/playground-v1-web",
    "dev:api": "cd api && yarn dev",
    "dev:web": "cd web && yarn dev",
    "build": "cd api && yarn build && cd ../web && yarn build"
  }
}
