{
  "name": "@temporium/gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Tempo Gateway - Payments-first gateway for Tempo blockchain",
  "scripts": {
    "dev": "turbo run dev --filter=@temporium/gateway-api --filter=@temporium/gateway-web",
    "build": "turbo run build --filter=@temporium/gateway-api --filter=@temporium/gateway-web",
    "lint": "turbo run lint --filter=@temporium/gateway-api --filter=@temporium/gateway-web",
    "clean": "turbo run clean --filter=@temporium/gateway-api --filter=@temporium/gateway-web"
  }
}
